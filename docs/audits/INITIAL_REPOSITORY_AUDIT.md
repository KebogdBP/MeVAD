# Initial Repository Audit

**Проект:** MegaDownloader Video & Audio  
**Дата проверки:** 2026-07-25  
**Канонический remote:** `https://github.com/KebogdBP/MeVAD`  
**Результат:** greenfield repository

## Подтверждённые факты

- Remote существует и доступен.
- Remote не содержит `HEAD`, веток, тегов или коммитов.
- Локальный workspace изначально не являлся Git-репозиторием.
- В исходном workspace находился только `MEGADOWNLOADER_PROJECT_VISION.md`.
- Исходный код, зависимости и бинарники прежнего CLI отсутствуют.

## Вывод

MeVAD является новым репозиторием, а не checkout прежнего Video Downloader.
Поэтому аудит существующего CLI в рамках этого репозитория невозможен.
Упомянутые в Project Vision рабочие сценарии считаются продуктовыми
требованиями, пока они не подтверждены кодом и тестами.

## Решение для Phase 0

1. Инициализировать новый репозиторий документацией проекта. Выполнено.
2. Не создавать фиктивную структуру legacy-приложения.
3. Не переносить старый код без отдельного аудита источника и лицензии.
4. Начать чистую разработку `downloader-core`; legacy-код может быть
   импортирован позже только выборочно и отдельным решением.
5. Использовать Python 3.11 как минимально поддерживаемую версию.
6. После появления исполняемого кода дополнять этот аудит инвентаризацией
   конкретных модулей и зависимостей.

## Первый исполняемый baseline

Добавлены:

- пакет `src/mevad`;
- CLI-команды `doctor` и `validate-url`;
- доменные модели и исключения;
- синтаксический слой URL security;
- discovery FFmpeg/FFprobe;
- Ruff, strict mypy, pytest и pre-commit;
- GitHub Actions для Python 3.11 и 3.13.

Baseline от 2026-07-25:

```text
Ruff       passed
mypy       passed (strict)
pytest     20 passed
coverage   83%
CLI doctor passed
```

## Открытые вопросы

- Какой инструмент будет использоваться для lock-файла?
- Какая лицензия будет выбрана для open-source/self-hosted редакции?
